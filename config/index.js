const {
  BOT_TOKEN,
  DB_HOST,
  MONGODB_URI,
  MONGOLAB_AMBER_URI,
  MONGOLAB_COPPER_URI,
  PROD_MONGODB,
  PORT,
} = process.env;

const config = {
  BOT_TOKEN,
  DB_HOST,
  MONGODB_URI,
  MONGOLAB_AMBER_URI,
  MONGOLAB_COPPER_URI,
  PROD_MONGODB,
  PORT,
};

module.exports = config;